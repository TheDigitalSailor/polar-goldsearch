VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Shared secret — must match FUNCTION_SECRET in Supabase Edge Function secrets.
# Generate with: openssl rand -hex 32
# Leave blank to disable secret checking (not recommended in production).
VITE_FUNCTION_SECRET=
